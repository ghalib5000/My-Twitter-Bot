const twitterApp = {
  consumer_key:"12",// process.env.CONSUMER_KEY,
  consumer_secret:"12",// process.env.CONSUMER_KEY_SECRET,
  access_token:"12",// process.env.ACCESS_TOKEN,
  access_token_secret:"12",// process.env.ACCESS_TOKEN_SECRET,
  timeout_ms: 60 * 1000 // optional HTTP request timeout to apply to all requests.
};

module.exports = {
  twitterApp,
  userName: "12",// process.env.USERNAME
};
