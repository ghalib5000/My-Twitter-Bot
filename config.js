const twitterApp = {
  consumer_key:"hello",// process.env.CONSUMER_KEY,
  consumer_secret:"hello",// process.env.CONSUMER_KEY_SECRET,
  access_token:"hello",//  process.env.ACCESS_TOKEN,
  access_token_secret:"hello",// process.env.ACCESS_TOKEN_SECRET,
  timeout_ms: 60 * 1000 // optional HTTP request timeout to apply to all requests.
};

module.exports = {
  twitterApp,
  userName:"hello"//  process.env.USERNAME
};
